# This is a test to understand how the env vars work in a docker image 
The test consists in having an `ENV` param set into the Dockerfile with a value 
and after setting the same variable with `--env` flag.